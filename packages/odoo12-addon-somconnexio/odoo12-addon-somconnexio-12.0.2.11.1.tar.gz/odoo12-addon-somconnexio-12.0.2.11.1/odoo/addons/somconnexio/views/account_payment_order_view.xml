<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_payment_order_form" model="ir.ui.view">
        <field name="model">account.payment.order</field>
        <field name="inherit_id" ref="account_payment_order.account_payment_order_form"/>
        <field name="groups_id" eval="[(4, ref('queue_job.group_queue_job_manager'))]"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page name="page_set_to_uploaded_jobs" string="Set as uploaded Jobs" attrs="{'invisible': [('set_uploaded_job_ids', '=', [])]}">
                    <field name="set_uploaded_job_ids"
                           options="{'reload_on_button': true}"
                           nolabel="1"
                           readonly="1"
                           context="{'tree_view_ref': 'account_invoice_validation_queued.view_queue_job_account_invoice_validation'}"
                    />
                </page>
            </notebook>
        </field>
    </record>
</odoo>