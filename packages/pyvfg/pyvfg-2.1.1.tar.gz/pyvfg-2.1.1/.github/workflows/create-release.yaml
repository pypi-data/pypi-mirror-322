name: Create Release

on:
  workflow_dispatch:

jobs:
  create-release:
    name: Create Release
    uses: VersesTech/github-actions/.github/workflows/release-please.yaml@main
    secrets: inherit
    with:
      config-file: release-please-config.json
      manifest-file: .release-please-manifest.json
