[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "jinja_filters"
version = "0.1.0"
authors = [{name = "Shin Jin", email = "jin.shin@singtel.com"}]
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["description"]
requires-python = ">=3.10, <4.0"