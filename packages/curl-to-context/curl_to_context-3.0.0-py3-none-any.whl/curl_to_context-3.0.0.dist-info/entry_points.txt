[console_scripts]
c2c = curl_to_context.main:app convert -f context
c2g = curl_to_context.main:app convert -f grab
curl-to-context = curl_to_context.main:app
curl2context = curl_to_context.main:app convert -f context
curl2grab = curl_to_context.main:app convert -f grab

[gui_scripts]

