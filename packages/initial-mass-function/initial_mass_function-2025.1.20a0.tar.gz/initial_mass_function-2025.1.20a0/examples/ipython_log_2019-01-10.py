########################################################
# Started Logging At: 2019-01-10 09:17:50
########################################################

########################################################
# # Started Logging At: 2019-01-10 09:17:50
########################################################
import reproject
reproject.__version__
import sys
from distutils.version import StrictVersion
version = reproject.__version__
from distutils.version import StrictVersion,LooseVersion
LooseVersion(version) < "0.5"
LooseVersion(version) > "0.5"
LooseVersion(version)
(1e-15*u.sr).to(u.arcsec**2)**0.5
from astropy import constants, units as u, table, stats, coordinates, wcs, log, coordinates as coord, convolution, modeling; from astropy.io import fits, ascii; from astropy.table import Table
(1e-15*u.sr).to(u.arcsec**2)**0.5
(1e3*u.rad).to(u.arcsec)
(1e-3*u.rad).to(u.arcsec)
206/0.006
