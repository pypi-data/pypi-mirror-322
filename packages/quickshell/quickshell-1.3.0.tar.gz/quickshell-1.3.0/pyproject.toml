[project]
name = "quickshell"
version = "1.3.0"
description = "Removes the boilierplate when working with shells in python."
readme = "README.md"
authors = [
    { name = "Aaron2599" }
]
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
