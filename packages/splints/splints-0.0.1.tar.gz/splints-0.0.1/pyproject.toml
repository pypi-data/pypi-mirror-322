[build-system]
requires = ["setuptools"]

[project]
name = "splints"
version = "0.0.1"
dependencies = ["pydantic == 2.9.2", "pyyaml"]

[project.scripts]
splints = "splints.entrypoint:run"

[tool.setuptools.package-dir]
splints = "splints"


[tool.setuptools]
packages = ["splints"]
