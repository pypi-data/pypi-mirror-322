[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[metadata]
name = "cellSP"
version = "0.0.1"

[tool.setuptools.packages]
find = {}  # Scan the project directory with the default parameters

[project]
name = "cellSP"
version = "0.0.1"
authors = [
  { name="Bhavay Aggarwal", email="bhavayaggarwal07@gmail.com" },
]
description = "cellSP."
readme = "README.md"
requires-python = ">=3.12"