apiVersion: engine.h2o.ai/v1alpha1
kind: DriverlessAIVersion
metadata:
  # This DAIVersion object can be changed during tests. Do not use it!
  name: 1.10.5-do-not-use-me
spec:
  image: gcr.io/vorvan/h2oai/mockdai:latest
  imagePullPolicy: Always
  imagePullSecrets:
    - name: "image-pull-secret-name"
