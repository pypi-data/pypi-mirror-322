[tool.poetry]
name = "psswd-box-generator"
version = "0.1.3"
description = "Password generating library specifically built for use in my Psswd Box application."
authors = ["Melvin Quick <melvinquick@proton.me>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.12,<3.14"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
