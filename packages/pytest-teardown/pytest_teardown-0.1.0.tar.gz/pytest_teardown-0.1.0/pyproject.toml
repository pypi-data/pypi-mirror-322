[tool.poetry]
name = "pytest-teardown"
version = "0.1.0"
description = ""
authors = ["Albert Safin"]
readme = "README.md"
packages = [{include = "teardown", from = "src"}]

[tool.poetry.dependencies]
python = "^3.8"
pytest = "^8.3.3"
simple-singleton = "^2.0.0"

[tool.poetry.plugins.pytest11]
pytest-teardown = "teardown.plugin"


[tool.poetry.group.dev.dependencies]
pytest-xdist = "^3.6.1"
pytest-parallel = "^0.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
