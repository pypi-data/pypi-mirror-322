[package]
name = "py_execute_interop"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bytes = { version = "1.3.0", features = ["serde"] }
ipc-channel = "0.18.0"
serde = { version = "1.0", features = ["derive", "std"] }
