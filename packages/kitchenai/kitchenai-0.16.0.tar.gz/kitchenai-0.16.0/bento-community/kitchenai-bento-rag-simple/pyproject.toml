[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
name = "kitchenai_rag_simple_bento"
dynamic = ["version"]
description = ""
readme = "README.md"
authors = [
  { name = "epuerta", email = "esteban_puerta@rhinosearch.io" },
]
requires-python = ">=3.11"
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]
dependencies = [
  "django",
  "llama_index",
  "llama-index-llms-litellm",
  "llama-index-vector-stores-chroma",
  "kitchenai-llama",
  "chromadb",
  "boto3",
  "djp"
]


[tool.django-stubs]
django_settings_module = "kitchenai_rag_simple_bento.settings"

[tool.coverage.run]
source_pkgs = ["kitchenai_rag_simple_bento", "tests"]
branch = true
parallel = true

[tool.coverage.paths]
kitchenai_rag_simple_bento = ["*/kitchenai_rag_simple_bento/kitchenai_rag_simple_bento"]
tests = ["tests", "*/kitchenai_rag_simple_bento/tests"]

[tool.coverage.report]
exclude_lines = ["no cov", "if __name__ == .__main__.:", "if TYPE_CHECKING:"]


[tool.hatch.env]
requires = ["hatch-pip-compile>=1.11.2"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
exclude = ["static"]
artifacts = ["staticfiles"]

[tool.hatch.build.targets.binary]

[[tool.hatch.envs.all.matrix]]
python = ["3.11", "3.12"]

[tool.hatch.envs.default]
type = "pip-compile"
pip-compile-constraint = "default"
pip-compile-installer = "uv"
pip-compile-resolver = "uv"
lock-filename = "requirements.txt"

[tool.hatch.envs.dev]
dependencies = [
  "django-browser-reload",
  "django-debug-toolbar",
  "dj-notebook>=0.6.1",
  "Werkzeug[watchdog]>=3.0.1",
  "django-stubs[compatible-mypy]",
  "django-fastdev",
  "coverage[toml]>=6.5",
  "pre-commit",
  "pytest",
  "pytest-django",
  "pytest-sugar",
  "pytest-xdist",
  "falco-cli",
  "django-watchfiles",
  "git-cliff",
  "bump-my-version",
  "hatch-pip-compile",
  "jupyterlab",
  ]
lock-filename = "requirements-dev.txt"


[tool.hatch.envs.test]
dependencies = [
    "pytest-django",
    "pytest",
    "pytest-asyncio",
    "httpx",
    "coverage[toml]>=6.5",
]

[tool.hatch.envs.docs]
template = "dev"
extra-dependencies = [
  "furo",
  "sphinx",
  "sphinx-autobuild",
  "sphinx-copybutton",
  "sphinx-design",
  "myst-parser",
]
lock-filename = "docs/requirements.txt"

[tool.mypy]
python_version = "3.11"
check_untyped_defs = true
ignore_missing_imports = true
warn_unused_ignores = true
warn_redundant_casts = true
warn_unused_configs = true
plugins = ["mypy_django_plugin.main"]

[[tool.mypy.overrides]]
ignore_errors = true
module = [
  "boto3.*",
  "botocore.*",
  "django_filters.*",
  "djclick.*",
  "health_check.*",
  "*.migrations.*",
]

[tool.mypy_django_plugin]
ignore_missing_model_attributes = true

[project.entry-points.djp]
kitchenai_rag_simple_bento = "kitchenai_rag_simple_bento"

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "kitchenai.settings"
pythonpath = "."
asyncio_mode = "auto"

[tool.ruff.lint]
extend-ignore = ["TID252", "RUF012", "TRY003", "EM101", "I001", "I002"]

[tool.ruff.lint.extend-per-file-ignores]
"deploy/*" = ["INP001"]
"*/__main__.py" = ["S104"]
"*/migrations/*" = ["E501"]
"tests/**/*" = ["PLR2004", "S101", "TID252", "ARG001"]
"docs/conf.py" = ["INP001", "A001"]

[tool.bumpversion]
current_version = "0.0.1"
parse = "(?P<major>\\d+)\\.(?P<minor>\\d+)\\.(?P<patch>\\d+)"
serialize = ["{major}.{minor}.{patch}"]
search = "{current_version}"
replace = "{new_version}"
regex = false
ignore_missing_version = false
ignore_missing_files = false
tag = true
sign_tags = false
tag_name = "v{new_version}"
tag_message = "Bump version: {current_version} → {new_version}"
allow_dirty = false
commit = true
message = "Bump version: {current_version} → {new_version}"
commit_args = "--no-verify"

[[tool.bumpversion.files]]
filename = "pyproject.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'

[tool.git-cliff.changelog]
header = """
# Changelog\n
All notable changes to this project will be documented in this file.\n
"""
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}*({{ commit.scope }})* {% endif %}\
            {% if commit.breaking %}[**breaking**] {% endif %}\
            {{ commit.message | upper_first }}\
    {% endfor %}
{% endfor %}\n
"""
footer = """
<!-- generated by git-cliff -->
"""
trim = true

[tool.git-cliff.git]
conventional_commits = true
filter_unconventional = true
split_commits = false
commit_parsers = [
  { message = "^bento\\(simple-rag:feat\\)", group = "<!-- 0 -->🚀 Simple-RAG Features" },
  { message = "^bento\\(simple-rag:fix\\)", group = "<!-- 1 -->🐛 Simple-RAG Fixes" },
  { message = "^bento\\(simple-rag:refactor\\)", group = "<!-- 2 -->🚜 Simple-RAG Refactor" },
  { message = "^bento\\(simple-rag:docs\\)", group = "<!-- 3 -->📚 Simple-RAG Documentation" },
  { message = "^bento\\(simple-rag:perf\\)", group = "<!-- 4 -->⚡ Simple-RAG Performance" },
  { message = "^bento\\(simple-rag:style\\)", group = "<!-- 5 -->🎨 Simple-RAG Styling" },
  { message = "^bento\\(simple-rag:test\\)", group = "<!-- 6 -->🧪 Simple-RAG Testing" },
  { message = "^bento\\(simple-rag:build\\)", group = "<!-- 7 -->📦 Simple-RAG Build" },
  { message = "^bento\\(simple-rag:chore\\)", group = "<!-- 8 -->⚙️ Simple-RAG Tasks" },
  { message = "^bento\\(simple-rag:security\\)", group = "<!-- 9 -->🛡️ Simple-RAG Security" }
]
protect_breaking_commits = false
filter_commits = false
topo_order = false
sort_commits = "oldest"

[tool.hatch.version]
path = "kitchenai_rag_simple_bento/__version__.py"
