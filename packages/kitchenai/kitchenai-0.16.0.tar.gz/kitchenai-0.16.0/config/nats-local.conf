# Basic configuration for local development
host: 0.0.0.0
port: 4222
http_port: 8222

# JetStream configuration
jetstream {
    store_dir: /data
    max_memory_store: 1G
    max_file_store: 10G
}

# Allow all connections without authentication
authorization {
    # No authentication required
    # Any client can connect and access all subjects
} 