[tool.poetry]
name = "pureauth-pysdk"
version = "2.1.2"
description = "pureauth-sdk contains all functions and classes to work with PureAUTH authentication platform"
authors = ["PureID Labs <hello@pureid.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
setuptools = "^69.1.1"
requests = "^2.31.0"
ecdsa = "^0.18.0"
cryptography = "^42.0.5"

[tool.poetry.group.dev.dependencies]
black = "^24.2.0"
flake8 = "^7.0.0"
ipykernel = "^6.29.3"

[build-system]
requires = ["poetry-core", "setuptools"]
build-backend = "poetry.core.masonry.api"
