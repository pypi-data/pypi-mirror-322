from setuptools import setup, find_packages

setup(
    name="pymoskey",
    version="0.5",
    packages=find_packages(),
    install_requires=["pyautogui"],  # Ajoutez ici les dépendances nécessaires
)
