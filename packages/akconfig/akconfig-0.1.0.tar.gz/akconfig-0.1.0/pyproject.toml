[tool.poetry]
name = "akconfig"
version = "0.1.0"
description = "A configuration management for python projects."
authors = ["dapk <dapk@gmx.net>"]
license = "MIT"
readme = "README.md"
packages = [{include = "ak"}]

[tool.poetry.scripts]
basic = "examples.basic:main"

[tool.poetry.dependencies]
python = "^3.10"
terminaltables = "^3.1.10"
colored = "^2.2.4"

[tool.poetry.group.dev.dependencies]
black = "^24.10.0"
isort = "^5.13.2"
pytest = "^8.3.4"
click = "^8.1.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
